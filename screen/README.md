# screen/ — STUB (step 2 of 6)

LLM-assisted title/abstract screening against inclusion criteria. Not built yet.
Will read from `data/db/records.db` and write screening decisions back. Per the
engineering standard, LLM calls here route through local Claude Code, not a
hosted API.

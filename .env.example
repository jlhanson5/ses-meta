# Copy to .env and fill in. NEVER commit .env. Keys are read from the environment only.
# PubMed E-utilities key (raises rate limit 3/s -> 10/s). Optional but recommended.
NCBI_API_KEY=
# Semantic Scholar Graph API key (raises shared-pool limit). Optional.
S2_API_KEY=

# site/ — STUB (steps 5-6 of 6)

Static site for the continuously updating meta-analysis, plus the frozen
snapshot build for the journal article. Not built yet. `first_seen_run` on each
record is what the site will use to show what is new since the last run.
